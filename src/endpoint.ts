import ModelBuilder from './EndpointModel';
import { Express } from 'express';

export default class Endpoint {
    /**
     *
     */
    constructor(expressApp: Express, route: string, ModelBuilder: ModelBuilder) {

    }
}